import React from "react";

import { ProfileInfo } from "../../components/user/ProfileInfo";

export const HomePage = () => (
    <div id="home-page" className="container-fluid container-main">
        <ProfileInfo />
    </div>
);
